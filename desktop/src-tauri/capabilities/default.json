{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability set for the local studio shell.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
